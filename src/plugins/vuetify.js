import Vue from 'vue'
import Vuetify from 'vuetify/lib'
Vue.use(Vuetify)
export default new Vuetify({
// eslint-disable-next-line indent
    icons: {
    // eslint-disable-next-line indent
        iconfont: 'md'
    // eslint-disable-next-line indent
    }
})
